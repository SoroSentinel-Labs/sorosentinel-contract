# Run from inside this repo, after `gh auth login`
gh issue create --title "Role separation between monitor and responder agents" --body "authorize_agent in src/lib.rs currently grants full flagging rights to every agent.`n`nRequirements:`n- Add a Role enum (Monitor, Responder)`n- authorize_agent takes a Role parameter and stores it per agent`n- flag_anomaly requires the caller to hold the Responder role`n`nAcceptance Criteria:`n- [ ] Role enum added and stored per agent`n- [ ] flag_anomaly rejects Monitor-role callers`n- [ ] Existing tests updated, new test for role rejection added"
gh issue create --title "Persist anomaly flag history on-chain" --body "flag_anomaly only emits an event today; no way to query past flags for a subject on-chain.`n`nRequirements:`n- Add persistent storage for flags, keyed by subject address`n- Add a get_flags(subject) -> Vec<Flag> read function`n`nAcceptance Criteria:`n- [ ] Flags persisted and queryable by subject`n- [ ] Test covering multiple flags for the same subject"
gh issue create --title "Contract upgrade safeguard" --body "No upgrade path exists yet for the deployed contract.`n`nRequirements:`n- Add an upgrade(new_wasm_hash) function, admin-only, following Soroban's standard upgrade pattern`n`nAcceptance Criteria:`n- [ ] Non-admin calls to upgrade panic`n- [ ] Test simulating an upgrade"
gh issue create --title "Unit tests for authorize_agent + flag_anomaly happy path" --body "src/test.rs only covers initialize.`n`nRequirements:`n- Add a test authorizing an agent and successfully flagging a subject`n`nAcceptance Criteria:`n- [ ] Test passes and covers both calls in sequence"
gh issue create --title "Unit test for unauthorized flag_anomaly rejection" --body "No test confirms a non-agent address is rejected by flag_anomaly.`n`nRequirements:`n- Add a test where a non-agent calls flag_anomaly and expect a panic`n`nAcceptance Criteria:`n- [ ] Test passes, confirms panic on unauthorized call"
gh issue create --title "Unit test for double-initialize panic" --body "No test confirms initialize can't be called twice.`n`nRequirements:`n- Add a test calling initialize twice and expect a panic on the second`n`nAcceptance Criteria:`n- [ ] Test passes, state from the first call is unchanged"
gh issue create --title "GitHub Actions CI: cargo build + test + clippy" --body "ci.yml exists but should be reviewed/extended as the contract grows.`n`nRequirements:`n- Review current workflow, add cargo registry caching`n`nAcceptance Criteria:`n- [ ] CI run time improved via caching`n- [ ] Workflow still passes on a clean PR"
